# -*- ruby -*-

require 'rubygems'
require 'hoe'

Hoe.spec 'rhessus' do
  # developer('FIX', 'FIX@example.com')

  # self.rubyforge_name = 'rhessusx' # if different than 'rhessus'
end

# vim: syntax=ruby
